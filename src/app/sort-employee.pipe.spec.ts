import { SortEmployeePipe } from './sort-employee.pipe';

describe('SortEmployeePipe', () => {
  it('create an instance', () => {
    const pipe = new SortEmployeePipe();
    expect(pipe).toBeTruthy();
  });
});
